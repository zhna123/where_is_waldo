function Dashboard({ imageUrl }) {
    return (
        <div>
            <p>Where's Waldo</p>
            <img src={ imageUrl } alt='' width='800px'></img>
        </div>
    )
}

export default Dashboard;